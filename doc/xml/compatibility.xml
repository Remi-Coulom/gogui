<chapter id="compatibility">
  
<title>Compatibility</title>
  
<para>
GoGui will work with all programs that can understand the most basic commands
of version 1 or 2 of the
<ulink url="http://www.lysator.liu.se/~gunnar/gtp/">Go Text Protocol</ulink>.
</para>
  
<section>
<title>Required commands</title>

<para>
It is required that the Go program can handle the following commands:
</para>
    
<itemizedlist>
<listitem>
<simpara>
<userinput>boardsize</userinput>
(<userinput>boardsize</userinput>, <userinput>clear_board</userinput>)
</simpara>
</listitem>
<listitem>
<simpara>
<userinput>black</userinput>, <userinput>white</userinput>
(<userinput>play</userinput>)
</simpara>
</listitem>
<listitem>
<simpara>
<userinput>genmove_black</userinput>,
<userinput>genmove_white</userinput>
(<userinput>genmove</userinput>)
</simpara>
</listitem>
</itemizedlist>
    
<para>
The commands in braces are used if the program supports
the <userinput>protocol_version</userinput> command and responds with 2.
</para>
    
<para>
In particular the Go program is not required to support the
<userinput>loadsgf</userinput> command.
GoGui translates all moves, setup and handicap stones into a series of
<userinput>black</userinput>, <userinput>white</userinput>
(<userinput>play</userinput>) commands.
</para>
</section>
  
<section>
<title>Optional commands</title>
    
<para>
The following other commands are used by GoGui.
</para>
    
<itemizedlist>
<listitem>
<simpara>
<userinput>name</userinput>
</simpara>
</listitem>
<listitem>
<simpara>
<userinput>final_status_list dead</userinput>
</simpara>
</listitem>
<listitem>
<simpara>
<userinput>help</userinput>
(<userinput>list_commands</userinput>)
</simpara>
</listitem>
<listitem>
<simpara>
<userinput>komi</userinput>
</simpara>
</listitem>
<listitem>
<simpara>
<userinput>quit</userinput>
</simpara>
</listitem>
<listitem>
<simpara>
<userinput>scoring_system</userinput>
</simpara>
</listitem>
<listitem>
<simpara>
<userinput>time_settings</userinput>
</simpara>
</listitem>
<listitem>
<simpara>
<userinput>undo</userinput>
</simpara>
</listitem>
<listitem>
<simpara>
<userinput>version</userinput>
</simpara>
</listitem>
</itemizedlist>
    
<para>
If the Go program does not understand them you will still be able to play
games, but you cannot use all of the functionality of GoGui.
</para>
    
<note>
<simpara>
The command <userinput>scoring_system area/territory</userinput>
(from an early draft of GTP version 2) is used for setting Chinese/Japanese
rules, if the command is supported by the program.
Otherwise the rule settings are quietly ignored.
</simpara>
</note>

<para>
The following GoGui specific GTP extension commands are used if supported by
the program:
</para>

<variablelist>
<varlistentry>
<term><userinput>gogui_interrupt</userinput></term>
<listitem>
<para>
See <xref linkend="interrupt"/>.
</para>
</listitem>
</varlistentry>
<varlistentry>
<term><userinput>gogui_sigint</userinput></term>
<listitem>
<para>
See <xref linkend="interrupt"/>.
</para>
</listitem>
</varlistentry>
<varlistentry>
<term><userinput>gogui_title</userinput></term>
<listitem>
<para>
For providing a window title, which may be game specific or include
a player name that is not known at program creation time.
The title is queried and set after a new game is started.
</para>
</listitem>
</varlistentry>
</variablelist>

<para>
<!-- Empty paragraph to enforce some space in JEditorPane (Java 1.5.0) -->
</para>

</section>
  
</chapter>
