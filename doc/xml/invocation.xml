<chapter id="invocation">
  
<title>Invocation</title>

<para>
<application>GoGui</application> can be invoked with the command:
<blockquote>
<simpara>
<userinput>java -jar gogui.jar [options] [program]</userinput>
</simpara>
</blockquote>  
The argument <userinput>program</userinput> must be a valid command line for
executing the actual Go program.
If no program is given, GoGui will start with the Go program attached
in the previous run or no Go program if there was none attached.
You can attach and detach a Go program at any time by using the
<guimenuitem>Attach Program</guimenuitem> and
<guimenuitem>Detach Program</guimenuitem> menu item from the
<guimenu>File</guimenu> menu.
</para>

<para>
See <xref linkend="reference-gogui"/> for a complete listing of the possible
options to <userinput>gogui</userinput>.
</para>

<para>
<note>
<title><application>GoGui</application> on Unix systems</title>
<para>
For Unix systems there are launcher scripts in the
<filename class="directory">bin</filename> directory.
The launcher scripts respect the environment variable
<userinput>JAVA_HOME</userinput>, if defined, and look for the jar files
in <filename class="directory">../lib</filename>
relative to the <filename class="directory">bin</filename> directory.
You can add the <filename>bin</filename> directory of GoGui to your
<userinput>PATH</userinput> environment variable.
The invocation is then simply:
<blockquote>
<simpara>
<userinput>gogui [options] program</userinput>
</simpara>
</blockquote>  
To register <application>GoGui</application> as a handler
for <ulink url="http://www.red-bean.com/sgf/">SGF</ulink> files,
you can configure a new file association for type
<userinput>application/sgf</userinput> and filename pattern
<userinput>*.sgf</userinput> to execute
<userinput>gogui&#160;""&#160;-file</userinput>
</para>
</note>
</para>

</chapter>
