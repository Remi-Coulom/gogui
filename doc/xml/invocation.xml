<chapter id="invocation">
  
  <title>Invocation</title>

  <para>
    <application>GoGui</application> can be invoked with the command:
    <blockquote>
      <userinput>
        <command>java -jar gogui.jar</command>
        <arg><replaceable>options</replaceable></arg>
	<arg><replaceable>program</replaceable></arg>
      </userinput>
    </blockquote>  
    The argument <arg choice="plain"><replaceable>program</replaceable></arg>
    must be a valid command line for executing the actual Go program.
    If no program is given, GoGui will open a dialog and ask for a
    command.
    If the empty string "" is given as a command,
    GoGui will start without any Go program.
  </para>

  <para>
    See
    <xref linkend="reference-gogui">
      <citerefentry><refentrytitle>gogui</refentrytitle></citerefentry>
    </xref>
    for a listing of the possible options.
  </para>

  <para>
  <note>
    <title><application>GoGui</application> on Unix systems</title>
    <para>
      On Unix systems, it is useful to edit the shell script
      <filename>gogui</filename> to use the locations of
      <filename>gogui.jar</filename> and the correct version
      of <filename>java</filename> and put it somewhere in your
      path. The invocation is then simply:
      <blockquote>
        <userinput>
          <command>gogui</command>
          <arg><replaceable>options</replaceable></arg>
          <arg><replaceable>program</replaceable></arg>
        </userinput>
      </blockquote>  
      To register <application>GoGui</application> as a handler
      for <ulink url="http://www.red-bean.com/sgf/">SGF</ulink> files,
      you can configure a new file association for type
      <userinput>application/sgf</userinput> and filename pattern
      <userinput>*.sgf</userinput> to execute
      <userinput>gogui&#160;-file</userinput>
    </para>
  </note>
  </para>

</chapter>