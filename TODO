BUGS:

GtpSynchronizer: don't enter outOfSync state if first command fails

TwoGtp: should not query game result if game was aborted due to a failure
response

MAYBE LATER:

GtpRegress: change into gtp test file directory, optional?

GtpRegress: improve documentation (in/out file locations, relative paths)

latex export current variation, not main

latex export using igo.sty (optional)
